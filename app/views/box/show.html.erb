<div class='sidebar'>
  <strong>
    <div class='sidebar-item'>
      <%= link_to 'root', '/' %> >
    </div>
    <div>
      <%= @box.name %>
    </div>
  </strong>
  <div class='stack-buttons-wrapper'>
    <div id='box-own' data-box-id='<%= @box.id%>' class='card-button stack-button'>own</div>
    <div id='box-disown' data-box-id='<%= @box.id%>' class='card-button stack-button'>disown</div>
  </div>
  <%= render partial: '/card/card_name_list', locals: { cards: @cards } %>
</div>

<div class='window'>
  <%= render partial: '/card/cards', locals: { cards: @cards }%>
</div>